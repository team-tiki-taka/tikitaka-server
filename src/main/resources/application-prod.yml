spring:
  # .env import
  config:
    activate:
      on-profile: local
  datasource:
    redis:
      host: ${REDIS_HOST}
      port: ${REDIS_PORT}

  jpa:
    # local - create
    # update - dev
    # none - prod
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        show_sql: false
        format_sql: false

